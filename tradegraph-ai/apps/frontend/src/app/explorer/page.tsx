import { PlaceholderPage } from "@/components/placeholder-page"
export default function ExplorerPage() { return <PlaceholderPage eyebrow="Explorer" title="Trade flows, ready to be explored." description="Filters, maps, and trade metrics arrive after the data model and ingestion pipeline are validated." /> }
